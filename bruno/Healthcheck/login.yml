info:
  name: login
  type: http
  seq: 4

http:
  method: POST
  url: http://{{host}}:{{apiPort}}/v1/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
