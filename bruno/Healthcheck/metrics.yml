info:
  name: metrics
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{metricsPort}}/metrics
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
